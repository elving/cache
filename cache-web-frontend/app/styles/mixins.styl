placeholder(color) {
  &::-webkit-input-placeholder {
  	color: color;
  }

  &:-moz-placeholder {
		color: color;
  }

  &::-moz-placeholder{
		color: color;
	}

  &:-ms-input-placeholder {
		color: color;
  }
}

flexCenter() {
  & {
    display: flex;
    align-items: center;
    justify-content: center;
  }
}
